"""SUBMISSION_CODE_PACKAGE hybrid src helpers."""
# Explicit package init — the original repository uses an implicit
# namespace package, but bundling it as a proper package makes the
# import behavior identical regardless of interpreter start-up flags.
